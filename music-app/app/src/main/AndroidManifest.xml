<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 添加权限用于显示通知 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/music_video"
        android:label="@string/app_name"
        android:roundIcon="@drawable/music_video"
        android:supportsRtl="true"
        android:theme="@style/Theme.Musicapp"
        tools:targetApi="31">

        <provider
            android:name=".model.provider.MusicContentProvider"
            android:authorities="com.liaowei.music.provider"
            android:enabled="true"
            android:exported="true">
        </provider>

        <service
            android:name=".service.MusicForegroundService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            >
            <intent-filter>
                <action android:name="com.liaowei.music.service.MusicForegroundService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.liaowei.music.service.MusicService" />
            </intent-filter>
        </service>

        <activity
            android:name=".PlayingActivity"
            android:exported="true" />
        <activity
            android:name=".SingerActivity"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".SongListActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>